apiVersion: apps/v1
kind: DaemonSet
metadata:
  name: talos-nvidia-validation-bridge
  namespace: nvidia-gpu-operator
spec:
  selector:
    matchLabels:
      app: talos-nvidia-validation-bridge
  template:
    metadata:
      labels:
        app: talos-nvidia-validation-bridge
    spec:
      automountServiceAccountToken: true
      containers:
        - args:
            - mkdir -p /run/nvidia/validations /run/nvidia/driver && touch /run/nvidia/validations/toolkit-ready /run/nvidia/driver/driver-ready && while true; do sleep 3600; done
          command:
            - /bin/sh
            - -c
          image: busybox:1.37
          name: bridge
          securityContext:
            privileged: true
          volumeMounts:
            - mountPath: /run/nvidia/validations
              name: run-nvidia-validations
            - mountPath: /run/nvidia/driver
              name: run-nvidia-driver
      nodeSelector:
        nvidia.com/gpu.present: "true"
      terminationGracePeriodSeconds: 0
      volumes:
        - hostPath:
            path: /run/nvidia/validations
            type: DirectoryOrCreate
          name: run-nvidia-validations
        - hostPath:
            path: /run/nvidia/driver
            type: DirectoryOrCreate
          name: run-nvidia-driver
