apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: fleet-controller-bootstrap
  namespace: fleet
rules:
  - apiGroups:
      - "*"
    resources:
      - "*"
    verbs:
      - "*"
