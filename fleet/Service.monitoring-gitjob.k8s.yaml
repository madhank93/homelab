apiVersion: v1
kind: Service
metadata:
  labels:
    app: gitjob
  name: monitoring-gitjob
  namespace: fleet
spec:
  ports:
    - name: metrics
      port: 8081
      protocol: TCP
      targetPort: 8081
  selector:
    app: gitjob
    fleet.cattle.io/shard-default: "true"
  type: ClusterIP
